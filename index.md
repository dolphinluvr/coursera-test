# Welcome to dolphinluvr/coursera-test

This is a simple GitHub Pages site for the repository.

You can edit this `index.md` to change the site content. To publish:

- Add more Markdown files or HTML assets as needed.
- Enable GitHub Pages in the repository Settings → Pages and select the `main` branch and `/` (root) folder as the source.

If you prefer the site under `docs/`, move this file to `docs/index.md` and publish from the `/docs` folder.

Happy publishing!